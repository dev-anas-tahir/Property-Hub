# Property Hub - Agent Guide

This document provides AI agents with essential context for making informed decisions when working on the Property Hub codebase. It includes technology stack documentation, implementation patterns, and references to detailed project documentation.

---

## Tech Stack & Official Documentation

### Backend Framework
- **Django 6.0.2** - [Official Documentation](https://docs.djangoproject.com/en/stable/)
- **Python 3.13+** - [Official Documentation](https://docs.python.org/3.13/)
- **PostgreSQL 17** - [Documentation](https://www.postgresql.org/docs/17/)
- **Redis** - [Documentation](https://redis.io/documentation)

### Django Extensions & Libraries
- **Django Channels 4.3.2** - [Documentation](https://channels.readthedocs.io/en/stable/) - WebSocket support
- **Django Unfold 0.80.2** - [Documentation](https://unfoldadmin.com/docs/) - Modern admin interface
- **Django Axes 8.3.1** - [Documentation](https://django-axes.readthedocs.io/) - Brute-force protection
- **Django Storages 1.14.6** - [Documentation](https://django-storages.readthedocs.io/) - Cloud storage backends
- **WhiteNoise 6.11.0** - [Documentation](http://whitenoise.evans.io/) - Static file serving
- **Daphne 4.2.1** - [Documentation](https://github.com/django/daphne) - ASGI server
- **Environs 14.6.0** - [Documentation](https://environs.readthedocs.io/) - Environment variable parsing

### Frontend
- **Tailwind CSS 4** - [Documentation](https://tailwindcss.com/docs) - Utility-first CSS
- **DaisyUI** - [Documentation](https://daisyui.com/docs/) - Component library
- **Django Cotton 2.7.2** - [Documentation](https://django-cotton.com/) - Template components
- **django-tailwind-cli 4.6.2** - Node-free Tailwind CLI integration
- **HTMX** - [Documentation](https://htmx.org/docs/) - Dynamic HTML interactions
- **Alpine.js** - [Documentation](https://alpinejs.dev/guide) - Lightweight JavaScript framework

### Infrastructure & DevOps
- **Docker** - [Documentation](https://docs.docker.com/) - Containerization
- **AWS S3** - [Documentation](https://docs.aws.amazon.com/s3/) - Object storage
- **Localstack** - [Documentation](https://docs.localstack.cloud/) - Local AWS simulation

### Package Management
- **UV** - [Documentation](https://docs.astral.sh/uv/) - Fast Python package manager

---

## Project Documentation Index

All implementation details are documented in the `docs/` directory:

### Architecture
- **[Architecture Overview](./docs/architecture/overview.md)** - System design, tech stack, data flow, security, and scalability
- **[Frontend Architecture](./docs/architecture/frontend.md)** - Django Tailwind CLI, Cotton, static assets

### Development Guides
- **[Frontend Setup](./docs/development/frontend-setup.md)** - Tailwind CSS workflow, asset management, development commands
- **[Chat Client](./docs/development/chat-client.md)** - WebSocket client API, reconnection strategy, message format

### Operations
- **[Deployment Checklist](./docs/deployment/checklist.md)** - Pre-deployment, build verification, post-deployment steps

### Process
- **[Contributing Guide](./docs/guides/contributing.md)** - Development workflow, code style, testing guidelines

---

## Project Structure

```
Property-Hub/
├── apps/                    # Django applications
│   ├── properties/         # Property listings & management
│   ├── users/              # Authentication & profiles
│   ├── chat/               # Real-time chat (Channels)
│   └── shared/             # Shared utilities
├── config/                 # Project configuration
│   ├── settings/          # Environment-specific settings
│   ├── urls.py            # Root URL routing
│   ├── asgi.py            # ASGI application (WebSocket support)
│   └── wsgi.py            # WSGI application
├── templates/              # HTML templates
│   ├── _layouts/          # Base layouts (base.html, auth.html, dashboard.html)
│   ├── cotton/            # Shared Cotton components
│   │   ├── forms/         # Form components
│   │   ├── navigation/    # Navbar, footer
│   │   └── ui/            # Generic UI elements
│   ├── properties/        # Property pages
│   ├── users/             # User auth pages
│   └── chat/              # Chat pages
├── assets/                 # Source assets
│   └── css/
│       └── input.css      # Tailwind CSS 4 source
├── static/                 # Production static assets
│   ├── dist/              # Compiled CSS (output.css)
│   ├── images/            # Static images
│   └── js/                # Static JavaScript files
├── staticfiles/            # Collected static files (auto-generated by collectstatic)
├── media/                  # User uploads
├── docs/                   # Project documentation
├── docker-compose.yml      # Development services
├── Dockerfile              # Production container
├── pyproject.toml          # Python dependencies (UV)
└── justfile                # Development command shortcuts
```

---

## Key Implementation Patterns

### 1. Services, Selectors & Forms (HackSoftware Style Guide)

The project follows the [HackSoftware Django Style Guide](https://github.com/HackSoftware/Django-Styleguide). Every app has:

| File | Responsibility |
|---|---|
| `models.py` | Data shape only — no business logic |
| `services.py` | All **writes** — create/update/delete, raises `ApplicationError` |
| `selectors.py` | All **reads** — pure query functions, no mutation |
| `forms.py` | **I/O shape only** — field format/validation, no DB queries |
| `views.py` | **Thin** — call form → call service/selector → render |

**Rules:**
- Services use keyword-only args: `def user_create(*, email, password, ...)`
- Services call `full_clean()` before every `.save()`
- Domain violations raise `ApplicationError(message, extra={})` — never `ValidationError` from services
- Selectors are named `<entity>_<query>`: `conversation_list_for_user`, `property_get_with_related`
- Forms validate field shape (email format, passwords match) but never DB uniqueness — that's for services
- Views catch `ApplicationError` and surface it via `form.add_error(None, e.message)` or `HttpResponseForbidden`

**Shared infrastructure in `apps/shared/`:**
- `BaseModel`: abstract with `created_at` (db_index=True) + `updated_at`; inherit for new models
- `ApplicationError`: `message: str` + optional `extra: dict`

### 2. Frontend Build Strategy

The project has no Node.js or npm build pipeline. Tailwind CSS 4 is built by `django-tailwind-cli`, using Django settings and `assets/css/input.css`.

- **Source CSS**: `assets/css/input.css`
- **Compiled output**: `static/dist/output.css`
- **Django collects**: files from `static/`, never `assets/`
- **Browser libraries**: HTMX and Alpine.js load from pinned CDN URLs with SRI in `templates/_layouts/base.html`

**Build Commands:**
```bash
# Development (Django plus Tailwind watch mode)
just runserver

# Production (minified)
uv run python manage.py tailwind build --force

# Then collectstatic
uv run python manage.py collectstatic --noinput
```

**See:** [Frontend Architecture](./docs/architecture/frontend.md) | [Frontend Setup](./docs/development/frontend-setup.md)

### 3. Django Settings Organization

Settings are split by environment:
- `config/settings/base.py` - Common settings
- `config/settings/development.py` - Development overrides
- `config/settings/production.py` - Production overrides

Environment variables loaded from `.env`, `.env.dev`, or `.env.prod` using `environs`.

**Key Settings:**
```python
# Custom user model
AUTH_USER_MODEL = "users.User"

# WebSocket channel layer
CHANNEL_LAYERS = {
    "default": {
        "BACKEND": "channels_redis.core.RedisChannelLayer",
        "CONFIG": {"hosts": [(REDIS_HOST, REDIS_PORT)]},
    }
}

# Static files with WhiteNoise
STATIC_URL = "static/"
STATIC_ROOT = BASE_DIR / "staticfiles"
STATICFILES_DIRS = [BASE_DIR / "static"]
STORAGES = {
    "staticfiles": {
        "BACKEND": "whitenoise.storage.CompressedManifestStaticFilesStorage",
    },
}
```

### 4. Component-Based Templates

Templates use Cotton for reusable components and regular Django templates for layouts/pages:

```django
{% extends "_layouts/base.html" %}

{% block content %}
  <c-properties.property-card :property="property" />
{% endblock %}
```

**Component locations:**
- Shared forms: `templates/cotton/forms/`
- Shared navigation: `templates/cotton/navigation/`
- Shared UI: `templates/cotton/ui/`
- Property-specific components: `apps/properties/templates/cotton/properties/`

### 5. WebSocket Chat Implementation

The chat system uses Django Channels with Redis as the channel layer.

**Client:** `static/js/chat-client.js` - `ChatClient` class
- Exponential backoff reconnection (1s, 2s, 4s, 8s, 16s)
- Message validation (1-5000 characters)
- Callback system for messages and connection status

**Server:** `apps/chat/consumers.py` - ASGI consumer
- Handles WebSocket connections
- Broadcasts messages to conversation groups
- Persists messages to database

**See:** [Chat Client Documentation](./docs/development/chat-client.md)

### 6. Database & Migrations

- **Database**: PostgreSQL 17 (development uses Docker)
- **Migrations**: Located in each app's `migrations/` directory
- **Custom user model**: `apps/users/models.py` - `User` extends `AbstractUser`

**Important:** Run migrations after creating models:
```bash
python manage.py makemigrations
python manage.py migrate
```

### 7. Static Files & Media

- **Static files**: generated CSS, JS, and images in `static/`; source CSS in `assets/css/`
- **Media uploads**: User-uploaded files in `media/`
- **Production storage**: Optional AWS S3 for media/static via `django-storages`

**S3 Configuration:**
```python
USE_S3_MEDIA = env.bool("USE_S3_MEDIA", False)
USE_S3_STATIC = env.bool("USE_S3_STATIC", False)
```

---

## Security Considerations

1. **Authentication**: Custom `User` model with Django's auth system
2. **Brute-force protection**: Django Axes (10 attempts, 30min lockout)
3. **CSRF protection**: Enabled by default
4. **Password validation**: Django's validators (similarity, length, common, numeric)
5. **Session storage**: Redis (not database) for performance
6. **Secrets**: All secrets via environment variables (never hardcoded)
7. **Production**: Always set `DEBUG=False`, configure `ALLOWED_HOSTS`, enable HTTPS

**See:** [Architecture Overview - Security](./docs/architecture/overview.md#security)

---

## Development Workflow

### 1. Start Development Services (Docker)
```bash
just up  # Starts PostgreSQL, Redis, Mailhog, Localstack
```

### 2. Install Dependencies
```bash
just build  # Installs Python dependencies
```

### 3. Database Setup
```bash
just migrate  # Apply migrations
```

### 4. Start Django Server And Tailwind Watch Mode
```bash
just runserver
```

### 5. Access Application
```
http://127.0.0.1:8000
```

**See:** [Contributing Guide - Development Workflow](./docs/guides/contributing.md)

---

## Testing

### Python Tests
```bash
# Run all tests (SQLite — no local Postgres needed)
DATABASE_URL=sqlite:///test.db python manage.py test

# Run a specific app
DATABASE_URL=sqlite:///test.db python manage.py test apps.chat

# Run a specific test file
DATABASE_URL=sqlite:///test.db python manage.py test apps.chat.tests.test_services
```

Test conventions:
- Use `django.test.TestCase` for sync tests, `TransactionTestCase` for async/WebSocket
- Use `factory_boy` for test data — factories live in `apps/<app>/tests/factories.py`
- Split tests by layer: `test_services.py`, `test_views.py`, `test_consumers.py`, `test_admin.py`
- Never mock the database — tests hit SQLite directly

### Code Style
```bash
uv run ruff check .    # Lint check
uv run ruff format .   # Auto-format
```

### Quality Gates (run before every commit)
```bash
DATABASE_URL=sqlite:///test.db python manage.py check
DATABASE_URL=sqlite:///test.db python manage.py makemigrations --check
uv run ruff check .
uv run ruff format --check .
```

### Frontend Build Test
```bash
uv run python manage.py tailwind build --force
uv run python manage.py collectstatic --noinput --dry-run
```

### Chat Client Tests
Open `docs/development/chat-client-test.html` in browser or run programmatically with `runTests()`.

**See:** [Contributing Guide - Testing Guidelines](./docs/guides/contributing.md)

---

## Dependencies Reference

### Python (pyproject.toml)
**Core:**
- django==6.0.2
- channels==4.3.2
- channels-redis==4.3.0
- psycopg[binary]==3.3.3 (PostgreSQL)
- redis[hiredis]>=7.2.0

**Third-party:**
- django-unfold==0.80.2 (admin theme)
- django-axes==8.3.1 (security)
- django-storages==1.14.6 (S3)
- whitenoise==6.11.0 (static files)
- django-cotton==2.7.2 (template components)
- django-tailwind-cli==4.6.2 (Tailwind CLI integration)
- pillow==12.1.1 (image processing)
- environs[django]==14.6.0 (env vars)
- nh3>=0.3.3 (HTML sanitization)

**Dev:**
- daphne==4.2.1 (ASGI server)
- django-debug-toolbar==6.2.0
- django-extensions==4.1
- ruff==0.15.2 (lint/format)
- factory-boy>=3.3.0 (test factories)

**Prod:**
- gunicorn==25.1.0 (WSGI server)
- uvicorn[standard]==0.41.0 (ASGI server)
- django-compressor==4.6.0

---

## Important Notes for Agents

### Do NOT:
1. **Put business logic in views** — views call services/selectors, nothing more
2. **Put DB queries in forms** — forms validate shape only; uniqueness checks go in services
3. **Call `.save()` directly in views** — always go through a service
4. **Raise `ValidationError` from services** — raise `ApplicationError(message)` instead
5. **Skip `full_clean()`** before saving a model in a service
6. **Add** Node.js, npm, or local frontend package files without an explicit architecture change
7. **Add** source files to `STATICFILES_DIRS` - only `static/` should be collected
8. **Hardcode** secrets or environment-specific values - always use environment variables
9. **Commit** compiled `static/dist/output.css` if CI/CD builds it - but it's okay to commit for releases
10. **Edit** generated files in `staticfiles/` - this directory is auto-generated
11. **Change** `AUTH_USER_MODEL` after migrations are created - requires database migration strategy
12. **Disable** security features (CSRF, Axes) without explicit requirement and mitigation plan

### Do:
1. **Use services for writes, selectors for reads** — the single most important rule
2. **Inherit `BaseModel`** for new models (provides `created_at` + `updated_at`)
3. **Raise `ApplicationError`** for domain violations in services
4. **Use keyword-only args** in all service and selector functions
5. **Follow** the Cotton component structure in `templates/cotton/` and app-level `templates/cotton/<app>/`
6. **Use** Tailwind utility classes first, custom CSS only when necessary
7. **Add** custom CSS in `assets/css/input.css` within `@layer` directives
8. **Write** reusable components for repeated UI patterns
9. **Run** quality gates (`check`, `makemigrations --check`, `ruff check`, `ruff format --check`) before committing
10. **Use** `factory_boy` factories for test data — never inline `User.objects.create_*` in production tests
11. **Reference** existing documentation in `docs/` before making architectural changes
12. **Use** the `ChatClient` class for WebSocket communication (don't create raw WebSocket connections)
13. **Keep** settings in environment-specific files, not in code

---

## Quick Decision Guide

### Adding a new page?
1. Write a **service** in `apps/<app>/services.py` if it involves a write
2. Write a **selector** in `apps/<app>/selectors.py` if it involves a read
3. Add/update a **form** in `apps/<app>/forms.py` for input validation (shape only)
4. Create a **thin view** in `apps/<app>/views.py`: validate form → call service/selector → render
5. Add URL pattern in `apps/<app>/urls.py`
6. Create template in `templates/<app>/` using `_layouts/base.html`
7. Reuse Cotton components from `templates/cotton/` or app-level Cotton directories as needed

### Adding new styles?
1. Use Tailwind utility classes directly in HTML
2. For reusable styles, add to `assets/css/input.css` in `@layer components`
3. Run `just runserver` for watch mode or `uv run python manage.py tailwind build --force` for production CSS

### Adding JavaScript interactivity?
1. For simple UI: Use Alpine.js from the pinned CDN script
2. For dynamic content: Use HTMX (add to template)
3. For project-owned browser logic: Add direct scripts to `static/js/`
4. For WebSocket: Use `ChatClient` class (see `static/js/chat-client.js`)

### Need real-time features?
1. Use Django Channels (see `apps/chat/` as reference)
2. Define consumer in `consumers.py`
3. Add WebSocket routing in `config/routing.py`
4. Use `ChatClient` pattern on frontend

### Storing files?
1. **User uploads**: Use `MEDIA_ROOT` (or S3 if `USE_S3_MEDIA=True`)
2. **Static assets**: Place generated CSS, direct JS, images, and fonts in `static/`
3. **Source CSS**: Place in `assets/css/input.css`

### Changing database schema?
1. Update model in `apps/<app>/models.py`
2. Run `python manage.py makemigrations`
3. Run `python manage.py migrate`
4. Update forms, views, and templates as needed
5. Add admin configuration in `apps/<app>/admin.py`

### Need admin interface?
1. Register models in `apps/<app>/admin.py`
2. Use Django Unfold for modern UI (already configured)
3. Customize with `list_display`, `search_fields`, `list_filter`, etc.

---

## Quick Reference Links

### Django
- [Model Field Reference](https://docs.djangoproject.com/en/stable/ref/models/fields/)
- [Class-Based Views](https://docs.djangoproject.com/en/stable/topics/class-based-views/)
- [Forms](https://docs.djangoproject.com/en/stable/topics/forms/)
- [Template Language](https://docs.djangoproject.com/en/stable/ref/templates/language/)
- [Static Files](https://docs.djangoproject.com/en/stable/howto/static-files/)

### Tailwind CSS
- [Utility Classes](https://tailwindcss.com/docs/all-classes)
- [Customization](https://tailwindcss.com/docs/configuration)
- [Dark Mode](https://tailwindcss.com/docs/dark-mode)

### DaisyUI
- [Components](https://daisyui.com/components/)
- [Theming](https://daisyui.com/docs/themes/)

### HTMX
- [Attributes](https://htmx.org/attributes/)
- [Examples](https://htmx.org/examples/)

### Alpine.js
- [Syntax](https://alpinejs.dev/guide/syntax)
- [Directives](https://alpinejs.dev/directives)

---

## Need More Information?

Consult the full documentation in `docs/`:
- **[Architecture Overview](./docs/architecture/overview.md)** - System design decisions
- **[Frontend Architecture](./docs/architecture/frontend.md)** - Build process details
- **[Frontend Setup](./docs/development/frontend-setup.md)** - Development workflow
- **[Chat Client](./docs/development/chat-client.md)** - WebSocket implementation
- **[Deployment Checklist](./docs/deployment/checklist.md)** - Production deployment
- **[Contributing Guide](./docs/guides/contributing.md)** - Development standards

For questions not covered, refer to the official documentation links above or the Django/JavaScript community resources.
